@model IEnumerable<Category>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h1 class="col-2 align-self-end ">
    <a class="btn btn-info" asp-action="Create">Add New</a>
</h1>
<div class="row">
    @foreach (var m in Model)
    {
        <div class="col-md-4">
            <div class="card card-hover">
                @if (m.Img != null)
                {
                    <img src="@Url.Content("~/images/categories/" + m.Img)" class="card-img-top" alt="Placeholder" />
                }
                <div class="card-body">
                    <h5 class="card-title">
                        @m.Name
                    </h5>
                    <a asp-action="Details" asp-route-id="@m.Id" class="stretched-link"></a>
                    <p class="card-text">
                        
                    </p>

                </div>
            </div>
        </div>

    }
    <br>
</div>

